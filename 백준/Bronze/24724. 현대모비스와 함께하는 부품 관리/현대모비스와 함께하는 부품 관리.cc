#include <iostream>
using namespace std;

int main() {
    int t, n, a, b;
    cin >> t;
    
    for(int k = 1; k <= t; k++) {
        cin >> n >> a >> b;
        for(int i = 0; i < n; i++) {
            int x, y;
            cin >> x >> y;
        }
        cout << "Material Management " << k << "\n";
        cout << "Classification ---- End!\n";
    }
}